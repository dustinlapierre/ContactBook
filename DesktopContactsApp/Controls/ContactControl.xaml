<UserControl x:Class="DesktopContactsApp.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopContactsApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="300">
    <Grid>
        <Border Background="#EEEEEE" CornerRadius="10">
            <StackPanel Margin="5">
                <TextBlock x:Name="nameText" 
                           Text="Name Last" FontSize="25" FontWeight="Bold"/>
                <TextBlock x:Name="emailText" 
                           Text="name@email.com" FontSize="15" Foreground="DodgerBlue"/>
                <TextBlock x:Name="phoneText" 
                           Text="1234-1234" FontSize="15" FontStyle="Italic"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
